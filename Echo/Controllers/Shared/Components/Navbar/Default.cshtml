@using Microsoft.Extensions.Localization
@using Microsoft.AspNetCore.Mvc.Localization

@using Echo

@inject IStringLocalizer<SharedResources> _

<nav class="navbar">
    <ul class="navbar-nav">
        @{
            bool isFirstLink = true;
        }
        @foreach (var link in Model)
        {
            <li class="@(isFirstLink ? "logo" : "nav-item")">
                @{
                    isFirstLink = false;
                }
                <a href="@link.RouteName" class="nav-link">
                    <i class="nav-icon uil @link.IconClass"></i>
                    <span class="link-text">@_[link.Text]</span>
                </a>
            </li>
        }
    </ul>
</nav>
